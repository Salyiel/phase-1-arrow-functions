// to define function divide
const divide = (nums, nums2) => {
    nums = 2000;
    nums2 = 100;
    return nums / nums2;
};

// to define function square and perform square root
const square = (num) => num * num;
square(5);

// to use arrow function to add two number
const add = (num1, num2) => num1 + num2;
add(6,6);